[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kalki"
version = "0.1.0"
description = "Autonomous SOC Analyst Platform — AI-driven alert triage, log correlation, threat hunting, and incident response"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.10"
authors = [
    {name = "Corey A. Wade", email = "corey@cwccie.com"},
]
keywords = ["security", "soc", "siem", "mitre-attack", "threat-hunting", "incident-response", "cybersecurity"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Information Technology",
    "Intended Audience :: System Administrators",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Security",
    "Topic :: System :: Monitoring",
    "Typing :: Typed",
]
dependencies = []

[project.optional-dependencies]
web = [
    "flask>=3.0",
    "flask-sock>=0.7",
]
cli = [
    "click>=8.0",
]
ml = [
    "numpy>=1.24",
]
yaml = [
    "pyyaml>=6.0",
]
all = [
    "kalki[web,cli,ml,yaml]",
]
dev = [
    "kalki[all]",
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "ruff>=0.4",
]

[project.scripts]
kalki = "kalki.cli:main"

[project.urls]
Homepage = "https://github.com/cwccie/kalki"
Repository = "https://github.com/cwccie/kalki"
Issues = "https://github.com/cwccie/kalki/issues"
Documentation = "https://github.com/cwccie/kalki#readme"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-v --tb=short"

[tool.ruff]
target-version = "py310"
line-length = 120

[tool.ruff.lint]
select = ["E", "W", "F", "I", "N", "UP"]

[tool.coverage.run]
source = ["kalki"]

[tool.coverage.report]
show_missing = true
skip_empty = true
